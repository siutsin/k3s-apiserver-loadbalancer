namespace: k3s-apiserver-loadbalancer-system

namePrefix: k3s-apiserver-loadbalancer-

resources:
- ../rbac
- ../manager
- metrics_service.yaml

patches:
- path: manager_metrics_patch.yaml
  target:
    kind: Deployment
